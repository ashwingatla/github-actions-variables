name: Workflow Demo

on: 
  # push
  schedule:
    - cron: '10 21 * * *'

env:
  ACCESS_ID: ${{ vars.ACCESS_ID }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Echo ACCESS_ID
      run: echo $ACCESS_ID
    